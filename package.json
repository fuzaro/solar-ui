{
  "name": "solar-ui",
  "version": "0.1.7",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "bash scripts/check-portal-base.sh && turbo run lint",
    "check-portal-base": "bash scripts/check-portal-base.sh",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "turbo": "^2.1.3",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.14.1"
}
